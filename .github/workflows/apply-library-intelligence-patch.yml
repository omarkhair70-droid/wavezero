name: Apply Library intelligence patch

on:
  push:
    branches:
      - feat/library-intelligence-20260916
    paths:
      - scripts/apply-library-intelligence-patch.py
      - .github/workflows/apply-library-intelligence-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'github-actions[bot]' }}
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/library-intelligence-20260916
          fetch-depth: 0

      - name: Apply focused Library patch
        run: python3 scripts/apply-library-intelligence-patch.py

      - name: Commit patch and remove one-shot tooling
        run: |
          rm -f scripts/apply-library-intelligence-patch.py
          rm -f .github/workflows/apply-library-intelligence-patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Connect artist and album browser to Library"
          git push origin HEAD:feat/library-intelligence-20260916
