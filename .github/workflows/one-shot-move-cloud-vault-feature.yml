name: One-shot move Cloud Vault feature

on:
  push:
    branches:
      - refactor/move-cloud-vault-feature

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/move-cloud-vault-feature
          fetch-depth: 0
      - name: Move Cloud Vault into feature ownership
        shell: bash
        run: |
          python tools/patch_move_cloud_vault_feature.py
          rm tools/patch_move_cloud_vault_feature.py
          rm .github/workflows/one-shot-move-cloud-vault-feature.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Move Cloud Vault into feature ownership'
          git push origin HEAD:refactor/move-cloud-vault-feature
