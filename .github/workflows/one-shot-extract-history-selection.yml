name: One-shot extract History selection

on:
  push:
    branches:
      - refactor/extract-history-selection

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-history-selection
          fetch-depth: 0
      - name: Route V3 through History selection policy
        shell: bash
        run: |
          python tools/patch_extract_history_selection.py
          rm tools/patch_extract_history_selection.py
          rm .github/workflows/one-shot-extract-history-selection.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Extract History selection from Flutter V3'
          git push origin HEAD:refactor/extract-history-selection
