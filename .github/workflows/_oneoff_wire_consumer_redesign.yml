name: One-off wire consumer redesign
on:
  push:
    branches: [redesign/consumer-wavezero-v1]
    paths:
      - tools/_oneoff_wire_consumer_redesign.py
      - .github/workflows/_oneoff_wire_consumer_redesign.yml
permissions:
  contents: write
jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/consumer-wavezero-v1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python tools/_oneoff_wire_consumer_redesign.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Wire focused consumer player into app shell"
          git push origin HEAD:redesign/consumer-wavezero-v1
