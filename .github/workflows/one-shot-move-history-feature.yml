name: One-shot move History feature

on:
  push:
    branches:
      - refactor/move-history-feature

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/move-history-feature
          fetch-depth: 0
      - name: Move Listening History into feature ownership
        shell: bash
        run: |
          python tools/patch_move_history_feature.py
          rm tools/patch_move_history_feature.py
          rm .github/workflows/one-shot-move-history-feature.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Move Listening History into feature ownership'
          git push origin HEAD:refactor/move-history-feature
