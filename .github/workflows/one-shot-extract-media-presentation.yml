name: One-shot extract media presentation

on:
  push:
    branches:
      - refactor/extract-media-presentation

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-media-presentation
          fetch-depth: 0
      - name: Move shared media presentation out of V3
        shell: bash
        run: |
          python tools/patch_extract_media_presentation.py
          rm tools/patch_extract_media_presentation.py
          rm .github/workflows/one-shot-extract-media-presentation.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Extract shared media presentation from Flutter V3'
          git push origin HEAD:refactor/extract-media-presentation
