name: One-shot Queue session restore extraction

on:
  push:
    branches:
      - refactor/extract-queue-session-restore

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-queue-session-restore
          fetch-depth: 0
      - name: Move Queue session restore policy out of V3
        shell: bash
        run: |
          python tools/patch_queue_session_restore.py
          rm tools/patch_queue_session_restore.py
          rm .github/workflows/one-shot-queue-session-restore.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add apps/flutter/wavezero_app/lib/app/wavezero_live_metrics_app_v3.dart tools/patch_queue_session_restore.py .github/workflows/one-shot-queue-session-restore.yml
          git commit -m 'Use extracted Queue session restore policy'
          git push origin HEAD:refactor/extract-queue-session-restore
