name: One-shot move Device Music feature

on:
  push:
    branches:
      - refactor/move-device-music-feature

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/move-device-music-feature
          fetch-depth: 0
      - name: Move Device Music into feature ownership
        shell: bash
        run: |
          python tools/patch_move_device_music_feature.py
          rm tools/patch_move_device_music_feature.py
          rm .github/workflows/one-shot-move-device-music-feature.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Move Device Music into feature ownership'
          git push origin HEAD:refactor/move-device-music-feature
