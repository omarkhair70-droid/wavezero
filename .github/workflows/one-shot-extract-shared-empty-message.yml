name: One-shot extract shared empty message

on:
  push:
    branches:
      - refactor/extract-shared-empty-message

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-shared-empty-message
          fetch-depth: 0
      - name: Move shared empty message out of V3
        shell: bash
        run: |
          python tools/patch_extract_shared_empty_message.py
          rm tools/patch_extract_shared_empty_message.py
          rm .github/workflows/one-shot-extract-shared-empty-message.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Extract shared empty message from Flutter V3'
          git push origin HEAD:refactor/extract-shared-empty-message
