name: One-off wire Queue panel

on:
  push:
    branches:
      - refactor/extract-queue-panel

permissions:
  contents: write

jobs:
  wire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-queue-panel
          fetch-depth: 0

      - name: Wire Queue panel
        run: python tools/wire_queue_panel.py

      - name: Remove one-off automation files
        run: |
          rm tools/wire_queue_panel.py
          rm .github/workflows/oneoff-wire-queue-panel.yml

      - name: Commit wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --exit-code && { echo "Expected wiring diff was empty"; exit 1; } || true
          git commit -m "Wire Queue consumer panel"
          git push origin HEAD:refactor/extract-queue-panel
