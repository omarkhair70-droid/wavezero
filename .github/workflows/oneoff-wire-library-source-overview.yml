name: One-off wire Library source overview

on:
  push:
    branches:
      - refactor/extract-library-source-overview

permissions:
  contents: write

jobs:
  wire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-library-source-overview
          fetch-depth: 0

      - name: Wire Library source overview
        run: python tools/wire_library_source_overview.py

      - name: Remove one-off automation files
        run: |
          rm tools/wire_library_source_overview.py
          rm .github/workflows/oneoff-wire-library-source-overview.yml

      - name: Commit wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --exit-code && { echo "Expected wiring diff was empty"; exit 1; } || true
          git commit -m "Wire Library source overview"
          git push origin HEAD:refactor/extract-library-source-overview
