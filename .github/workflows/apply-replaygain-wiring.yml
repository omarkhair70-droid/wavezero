name: Apply ReplayGain wiring

on:
  push:
    branches:
      - feat/sound-engine-replaygain-20260916
    paths:
      - scripts/apply_replaygain_wiring.py
      - .github/workflows/apply-replaygain-wiring.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/sound-engine-replaygain-20260916
          fetch-depth: 0
      - name: Apply ReplayGain wiring patch
        run: python3 scripts/apply_replaygain_wiring.py
      - name: Remove one-shot patch files
        run: git rm scripts/apply_replaygain_wiring.py .github/workflows/apply-replaygain-wiring.yml
      - name: Commit ReplayGain wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Wire ReplayGain loudness normalization"
          git push origin HEAD:feat/sound-engine-replaygain-20260916
