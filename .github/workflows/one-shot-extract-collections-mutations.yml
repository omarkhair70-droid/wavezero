name: One-shot extract Collections mutations

on:
  push:
    branches:
      - refactor/extract-collections-mutations

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-collections-mutations
          fetch-depth: 0
      - name: Route V3 through Collections mutation policy
        shell: bash
        run: |
          python tools/patch_extract_collections_mutations.py
          rm tools/patch_extract_collections_mutations.py
          rm .github/workflows/one-shot-extract-collections-mutations.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Extract Collections mutations from Flutter V3'
          git push origin HEAD:refactor/extract-collections-mutations
